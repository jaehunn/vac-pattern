import VTodoList from "./VTodoList";

type Props = {
  //
};

const TodoList = () => {
  const todoListProps = {
    //
  };

  return (
    <>
      <VTodoList {...todoListProps} />
    </>
  );
};

export default TodoList;
